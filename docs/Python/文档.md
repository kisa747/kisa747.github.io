# 文档

mkdocs.yml 添加以下内容：

```yaml
remote_branch: 'main'  # default gh-deploy
remote_name: 'git@github.com:kisa747/kisa747.github.io.git'  # default origin
```

运行命令将 site 目录推送

``` sh
uv run mkdocs gh-deploy
# 如果之前手动推送过，需要添加 --ignore-version 参数
uv run mkdocs gh-deploy --ignore-version
```

